spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/web_hospital_patient_card
    password: adminAndrew
    username: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  flyway:
    check-location: false